import { UniversitysearchPipe } from './universitysearch.pipe';

describe('UniversitysearchPipe', () => {
  it('create an instance', () => {
    const pipe = new UniversitysearchPipe();
    expect(pipe).toBeTruthy();
  });
});
